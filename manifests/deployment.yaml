apiVersion: apps/v1
kind: Deployment
metadata:
  name: will42
  namespace: dev
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: will42
  template:
    metadata:
      labels:
        app.kubernetes.io/name: will42
    spec:
      containers:
        - name: will42
          image: wil42/playground:v2
---
apiVersion: v1
kind: Service
metadata:
  name: will42
  namespace: dev
spec:
  ports:
      - protocol: TCP
        port: 8888
        targetPort: 8888
  selector:
    app.kubernetes.io/name: will42
  type: ClusterIP
