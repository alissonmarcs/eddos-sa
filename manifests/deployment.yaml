apiVersion: apps/v1
kind: Deployment
metadata:
  name: wil-playground
  namespace: dev
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: wil-playground
  template:
    metadata:
      labels:
        app.kubernetes.io/name: wil-playground
    spec:
      containers:
        - name: wil-playground
          image: wil42/playground:v1
---
apiVersion: v1
kind: Service
metadata:
  name: wil-playground
  namespace: dev
spec:
  ports:
      - protocol: TCP
        port: 8888
        targetPort: 8888
  selector:
    app.kubernetes.io/name: will-playground
  type: ClusterIP
